pipeline{
    agent any 
    stages{
        stage('stage1-git clone'){
            steps{
                #sh 'insert pipeline syntax here'
            }
        }
        stage('stage2-system update'){
            steps{
                sh 'sudo apt update -y'
            }
        }
    }
}
